scene:
  buffers:
    a: default
    b: default
  shaders:
    main:
      src: main.frag
      channels: u_buffer
    test:
      src: test.frag
      channels: u_buffer
  renderer:
    - main a -> b
    - main b -> a
    - test a
